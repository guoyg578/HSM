# HAM Station Manager fnOS V1.1 产品需求说明书（PRD）

> 面向开发人员的《HAM Station Manager fnOS V1.1 产品需求说明书（PRD）》。开发人员可以根据此文档进行架构设计、开发和测试。

## 1. 项目概述

### 1.1 项目名称

**HAM Station Manager（专业无线电电台管理系统）**

版本目标：

**fnOS V1.1 日常使用完整版**

部署环境：

- Docker容器

## 2. 产品定位

本软件不是传统意义上的单纯"通联日志软件（Logger）"，而是：

> 面向业余无线电爱好者的个人电台数字档案管理系统。

核心设计理念：

- 一个电台对应一个独立档案空间
- 电台固定信息（呼号、QTH、设备、功率、天馈）只需配置一次
- 每次通联自动继承本台信息
- 用户只记录变化的信息（对方信息、通联内容）
- 支持传统HAM日志表格模式，也支持现代时间线模式

产品形态参考：

- OneNote（目录管理方式）
- Excel（传统日志管理方式）
- NAS应用（长期存储方式）

## 3. 用户使用场景

### 场景1：家庭固定台

用户创建：

```
BG5XXX家庭电台
```

配置：

```
QTH：
安徽合肥

设备：
八重洲 FTM-150R

功率：
50W

天线：
钻石SG7700
```

以后每次通联：

系统自动填充：

```
我的呼号
我的QTH
我的设备
我的功率
我的天线
```

用户只输入：

```
时间
对方呼号
频率
模式
RST
对方QTH
备注
```

### 场景2：车载台

创建：

```
BG5XXX车载电台
```

配置：

```
设备：
ICOM IC-2730

天线：
车载天线

功率：
50W
```

切换电台后，所有QSO自动使用该配置。


## 5. 功能需求

## 模块一：电台管理（Station Management）

### 5.1 电台目录

界面采用类似OneNote目录结构。

左侧导航：

```
我的电台

├── 家庭电台
|
├── 车载电台
|
└── 便携电台
```

### 5.2 创建电台

字段：

| 字段 | 说明 |
| --- | --- |
| 电台名称 | 用户自定义 |
| 呼号 | CALLSIGN |
| QTH | 所在地 |
| Grid | 网格定位 |
| CQ Zone | 可选 |
| ITU Zone | 可选 |
| 备注 | 可选 |

### 5.3 电台配置

每个Station独立保存：

#### 设备

字段：

```
品牌
型号
类型
备注
```

示例：

```
八重洲
FTM-150R
车载电台
```

#### 天馈系统

字段：

```
天线名称
```

示例：

```
钻石SG7700
```

#### 功率配置

保存：

```
FM：
50W

数字模式：
20W

便携：
5W
```

## 模块二：QSO通联日志

### 6.1 新建QSO

新增通联时：

系统自动加载：

```
当前Station信息：

呼号
QTH
设备
天线
功率
```

用户无需重复输入。

### 6.2 QSO字段设计

数据库字段：

| 字段 | 说明 |
| --- | --- |
| id | 编号 |
| station_id | 所属电台 |
| 日期时间 | UTC |
| 对方呼号 | CALL |
| 频率 | MHz |
| 波段 | Band |
| 模式 | FM/SSB/CW/Digital |
| RST发送 | 我给对方 |
| RST收到 | 对方给我 |
| 对方QTH | 所在地 |
| 距离 | 公里 |
| 备注 | 文字 |
| 录音路径 | 可选 |
| QSL图片 | 可选 |

### 6.3 RST设计

必须拆分：

```
RST发送

RST收到
```

显示：

```
59 / 59

左：
我给出的报告

右：
对方给我的报告
```

## 模块三：日志显示方式

用户可自由切换。

顶部按钮：

```
[时间线模式]

[表格模式]
```

系统记忆用户选择。

## 7. 时间线模式

适合查看通联历史。

显示：

```
2026-08-04 20:35

BD5XXX

438.500MHz

FM

RST：

59 / 59

QTH：
上海

设备：
FTM-150R
```

支持显示：

- QSL图片
- 录音
- 地图

## 8. 表格模式

传统Excel布局。

示例：

| 日期 | 时间 | 呼号 | 频率 | 模式 | RST | QTH |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-08-04 | 20:35 | BD5XXX | 438.500 | FM | 59/59 | 上海 |

### 表格功能要求

#### 搜索

支持：

- 呼号搜索
- 日期搜索
- 频率搜索

#### 排序

支持：

- 日期
- 频率
- 距离

#### 自定义列

用户选择：

显示：

```
☑ 日期

☑ 呼号

☑ 频率

☑ 模式

☑ RST

☐ 设备

☐ 天线
```

## 模块四：快速记录

适用于：

- 开车
- 手持机
- 比赛

界面：

```
呼号：

频率：

模式：

RST：

保存
```

自动补充：

```
时间

设备

功率

天线
```

## 模块五：数据管理

### 9.1 数据存储

要求：

- NAS本地保存
- Docker Volume挂载
- 自动备份

目录：

```
/data

├── database

├── audio

├── qsl

└── export
```

### 9.2 ADIF支持

必须支持：

导入：

```
ADIF
```

导出：

```
ADIF
```

兼容：

- LoTW
- eQSL
- QRZ
- ClubLog

## 模块六：统计分析

首页显示：

```
累计QSO：

1280

本月：

86

最远距离：

9860km

常用频段：

430MHz

常用模式：

FM
```

## 10. UI设计要求

### 主界面

三栏布局：

```
--------------------------------------------------
左侧          中间             右侧

电台目录       QSO日志          当前配置

家庭电台       表格/时间线       FTM-150R

车载电台                         50W

便携电台                         SG7700
--------------------------------------------------
```

## 11. 技术要求

### 后端

推荐：

- Python
- FastAPI
- SQLAlchemy

### 前端

推荐：

- Vue3
- UI组件库@guoyg/k-ui

### 数据库

SQLite_

### 部署

必须支持：

Docker Compose


## 12. V1.1验收标准

完成以下功能视为交付：

- ✅ Docker一键部署
- ✅ 创建多个电台
- ✅ 设置设备/天线/功率
- ✅ 新建QSO自动继承本台信息
- ✅ RST发送/收到分离
- ✅ 表格模式
- ✅ 时间线模式
- ✅ 搜索过滤
- ✅ 数据持久化
- ✅ ADIF基础支持
